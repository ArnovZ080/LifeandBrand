DATABASE_URL=postgresql://stockuser:stockpass@localhost:5432/stockdb
SECRET_KEY=change-me-in-production

# Claude API (invoice OCR)
ANTHROPIC_API_KEY=

# Email — Office 365 example
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=stockalerts@yourdomain.com
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=stockalerts@yourdomain.com

# WhatsApp via Twilio (https://console.twilio.com)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=+14155238886

# Microsoft Teams — set per-contact in the UI (each manager has their own webhook)
# Get a webhook URL from: Teams channel → ... → Connectors → Incoming Webhook

# Micros POS
MICROS_API_URL=
MICROS_API_KEY=

# Sage
SAGE_API_URL=
SAGE_CLIENT_ID=
SAGE_CLIENT_SECRET=
